# 请求数量上限调整（2026-09-09）

按用户“不要设每天的上限，就让他自己跑”的要求，NBS、PBOC、SAFE 的 `max_requests_per_day` 和 `max_requests_per_run` 均设为 `null`，表示不限次数。请求间隔、单来源串行、缓存优先、robots 检查、TLS 校验、HTTP 熔断和连续失败停止机制保留。其他来源配置不变。

旧 NBS、SAFE 和隐藏监控进程通过停止标记退出，确认锁释放后重启。历史请求账本未清零，已完成窗口和归档未重置；NBS 清除旧检索断点中的预算耗尽提示，SAFE 清除已无效的次日恢复时间。新进程 NBS PID 12224、SAFE PID 16568、监控 PID 16780；10:58 两个下载任务均为 RUNNING，已开始新增请求。

PBOC 数量配置已更新，但原有 4 个种子队列已结束，两个受阻路径不会因配置调整自动重试；后续仍需拓展可访问来源。

验证：39 项针对性测试通过；311 份 NBS 已入库原稿离线回放通过，486 条独立核验候选、0 数值不一致、0 回放失败。回放结果和代码哈希见 `../pit_history_autorun/preflight.json`。解析逻辑本次未调整。

本次仅取消请求数量上限，有限队列和 7 天运行期限仍保留：本次 NBS 截止 9 月 16 日 10:58，SAFE 保留原截止 9 月 16 日 08:51。队列完成、来源受阻、程序异常、停止标记均可提前结束。关机/休眠时不推进，未配置开机自启动。此前按每日请求额度估算的排期已失效。

实时状态以 [latest.md](latest.md) 为准；归档成功不等于严格 PIT 观测已完成解析和入库。

10:59 实测 SAFE 从旧等待位置 `/safe/2013/0722/5012.html` 成功下载，HTTP 200，正文数 41 → 42，累计会话请求已超过原 50 次上限。请求账本和新日志均有新增。
