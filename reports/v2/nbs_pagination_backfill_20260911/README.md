# NBS 重复分页补查（2026-09-11）

原 154 个检索窗口已于 2026-09-10 22:40 结束，但下列 5 个年度窗口均在第 26 页重复返回第 1 页，未穷尽检索结果：

| 年份 | 关键词 | 原命中数 | 新补查范围 |
|---|---|---:|---|
| 2018 | 国民经济 | 598 | 一至四季度 |
| 2018 | 经济运行 | 650 | 一至四季度 |
| 2019 | 国民经济 | 526 | 一至四季度 |
| 2019 | 经济运行 | 576 | 一至四季度 |
| 2022 | 经济运行 | 511 | 一至四季度 |

## 执行方式

- 新增 20 个互不重叠、连续覆盖原全年范围的季度窗口。原 154 个完成窗口和 318 条入库记录保持不变；监控总数变为 174，新增补查单独记录在 `pagination_backfill`。
- 原来的逐页搜索、不可变原稿归档、已处理 URL 去重、独立证据校验及严格 PIT 补缺流程继续适用。
- 保留请求间隔、robots、熔断、空查询限次重试和 Windows 断点保存重试。单来源一个进程；每天和每轮请求数量不限。
- 某年度的四个季度全部完成且没有重复分页时，原待查项移入 `resolved_search_review`，保留原原因、子窗口名单和完成时间。子窗口若仍异常，父窗口不会被标记已解决。
- 该阶段只解决已发现的分页遗漏，不代表宏观历史数据已完整，也不替代 PBOC/SAFE 后续原稿解析。

## 验证与启动

- 52 项测试通过，覆盖日期完整覆盖、无重叠、错误父窗口或关键词拒绝、禁止提前解除待查标记，以及既有检索、断点和 PIT 校验回归。
- 启动前重放 363 份原稿，独立核验 804 条记录，0 不匹配、0 失败。
- `activation.json` 记录 09:02 的配置迁移；隐藏进程 PID 3332 从第一个补查窗口启动。
- `config_before.json`、`state_before.json`、`worker_before.py` 和 `preflight_before.json` 保留原状态；`config_proposed.json` 为审核过的 20 窗口清单，`preflight_after.json` 为通过的启动门禁。
- 第一季度实际返回与恢复状态记录在 `resume.json`；实时进度见 [监控](../source_monitor/latest.md)。

普通重启继续使用原断点；不要重复运行激活脚本。补查原始检索断点在 `reports/v2/pit_history_autorun/discovery/*_q*/search_state.json`，主断点仍为 `data/history_backfill/pit_history_autorun.json`。