# 本地原稿解析 — 2026-09-11

## 最新结果：SAFE 四字段已入库（2026-09-11 22:07）

独立原文核验 684 条全部通过，13 项测试通过；正式追加 584 条 B 级严格缺期，已有 96 条同值与超过截止日的 4 条未追加。旧 D 级记录完整保留，新增历史原始发布值与现行历史表之间的舍入/修订差异按数据库版本机制存储，不能把数据库 revision 计数解释为官方修订事件数。

银行结汇/售汇首次可用月末前移到 2013-03-31，涉外收入/对外付款前移到 2011-05-31；严格真实期数分别各 160/180。宽表 592 处变化均由本批记录解释，空值率由 51.03% 降到 46.17%。主库现为 770,480 条，中国 9,746 条，其他来源数量不变。

证据：`safe_monthly/independent_review.json`、`independent_evidence.json`、`prepare.json`、`ingestion.json`、`result.json`、`export_changes.csv`、`coverage_changes.csv`。剩余内部缺期见 `remaining_internal_gaps.csv`。`before/` 保存入库前中国观测及导出；`staged_panel/` 和 `published_panel/` 完全一致。NBS 未来续跑需核对外部入库后的基线。

以下为首轮解析阶段的历史记录，其中“未写主库”描述当时状态，当前以本节为准。

## 范围与当前状态

本批完全离线，不重新抓取，不处理 Wind，不写主库。对三个来源已归档正文逐篇核验原始文件大小和 SHA-256，再使用现有解析器回放。现有解析器的输出统一标记为未经核验候选，不因带有 PIT_A/B 标签就直接入库。

- 全量回放：`scripts/parse_archived_sources.py`；实时状态 `progress.json`，完成后生成 `summary.json`。
- 输入断点封存在 `inputs/`；候选输出 `unverified_candidates.parquet`，逐稿结果 `articles.csv`，异常 `held_articles.csv`。
- 全量回放包括 NBS 1,068 篇、PBOC 214 篇、SAFE 319 篇，共 1,601 篇。正文可能包含重复内容、解读或累计统计，原稿篇数不能当作有效记录数。

## SAFE 明确单月格式首轮

新增 `scripts/history/parse_safe_monthly_review.py`，仅提取官方元数据明确年/月、正文单句明确同一月份的美元金额。银行结售汇总额与银行代客结售汇分开；涉外收付款仍按银行代客口径。支持直接亿美元及人民币金额后明确注明的等值亿美元，换算为 bn_usd。只给出日期的稿件保守从次日零点可用。

- 319 篇中，182 篇提取 684 条候选，另 137 篇不适用此窄格式。
- 银行结汇/售汇各 161 期，2013-01 至 2026-07；涉外收入/对外付款各 181 期，2011-03 至 2026-07。这里是候选覆盖，不证明历史完整。
- 与当前严格主库及 2026-07-31 导出截止日对账：584 条缺期候选、96 条已有同值、4 条晚于截止日；无字段/数据期重复。
- 8 项测试通过，覆盖累计值、代客结售汇、人民币币种、冲突数值和年度标题拒绝。
- `safe_monthly/review_candidates.parquet`：候选；`safe_monthly/evidence.csv`：原句、匹配片段、月份锚点、口径、发布日期、原稿哈希；`safe_monthly/database_comparison.csv`：主库对账。

## 后续验收

先独立复核数值、月份、发布日期和历史口径，再对合格缺期做隔离库 PIT 边界与重复导入测试，最后提交主库并逐格对账导出。2010 年季度稿和全年稿中的单月值、旧银行代客口径、外储/社融及央行旧稿需要另行适配；不能用宽表前向填充掩盖缺期。

本目录产生的是解析与审核成果；抓取监控的待审计数尚未按本目录结果核销。不得把未经复核的候选直接提交主库。

## 全量回放完成（15:27）

| 来源 | 正文 | 产生候选的正文 | 候选条数 | 未通过现有解析器的正文 |
|---|---:|---:|---:|---:|
| NBS | 1,068 | 501 | 2,331 | 567 |
| PBOC | 214 | 42 | 113 | 172 |
| SAFE | 319 | 229 | 754 | 90 |

以上 3,198 条是旧解析器的未经核验候选，不与 SAFE 新规则的 684 条相加。未通过可能包括不含目标指标的解读稿、无匹配值或格式不支持，详情在 held_articles.csv，不能全部等同于原始文件损坏。

SAFE 新规则候选另通过内存隔离库重复导入（第二次 0 新增、684 不变）及 1,368 次日期边界检查；这仅证明结构和边界一致，不替代原文数值及口径的独立复核。主库前后均为 769,896 条，无写入。两个离线解析进程均已结束；后续应进入候选内容复核和旧格式适配。
