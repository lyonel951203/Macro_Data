# PBOC 断点保存恢复（2026-09-09）

## 故障与修复

16:38，财政部 2015 年“金融统计”检索第 1 页已成功下载并解析。worker 将 `state.json.tmp` 替换为 `state.json` 时遇到 Windows `WinError 5 / 拒绝访问`；异常退出时再次保存也失败，导致旧进程锁未清理，正式断点仍显示 RUNNING。

无法仅凭错误确定是哪一个进程占用了文件。此次修复针对 Windows 文件替换的暂时性拒绝访问和共享冲突：

- 写入独立的临时文件并 flush/fsync，避免后续保存覆盖已有恢复副本。
- 对 WinError 5、32、33 最多尝试替换 8 次，间隔从 50 毫秒逐步增加至 1 秒，累计等待上限 3.55 秒。
- 若仍无法替换，保留旧正式断点及完整临时恢复文件，继续报错退出；其他 I/O 错误不重试。
- 最终保存失败时仍执行锁清理，避免死进程锁阻止正常续跑。
- 未更改数据来源、网络限速、robots 或账户权限。

## 断点与证据

旧正式断点包含 264 个结果、72 个待抓链接；遗留临时文件包含 265 个结果、73 个待抓链接。核对所有旧结果均保留且未修改，全部 263 份 ARCHIVED 原稿的大小、路径范围和 SHA-256 一致。重放新增检索页后确认新增 2 个链接及剩余队列顺序与临时断点完全一致。

16:51 已恢复经验证的临时断点，保留请求计数 277、原截止时间 2026-09-16 08:51:28、历史阻断记录和全部队列；只清理已确认死亡 PID 2836 的旧锁。

- `state_before.json`：原正式断点。
- `pending_verified.json`：经核验的遗留临时断点；来源临时文件仍保留。
- `worker_lock_before.json`：旧死进程锁。
- `recovery.json`：恢复计数、时间与代码哈希。
- 执行脚本：`scripts/recover_pboc_checkpoint_20260909.py`；已恢复后拒绝重放旧断点。

## 验证与续跑

`pytest tests/test_source_workers.py tests/test_pboc_reprints.py -q`：**25 passed**。新增测试覆盖暂时拒绝访问后成功、重试耗尽仍保留旧断点和独立恢复文件、其他 I/O 错误立即抛出、最终保存失败仍清理 worker 锁。

通过 `scripts/start_source_workers.py --sources PBOC` 单独恢复 PBOC，PID **21584**。16:53:50 实际下载《货币增速仍在合理区间》，HTTP 200、25,387 字节，原文哈希与事件日志一致，正式断点推进到 266 个结果、72 个待抓链接（189 篇正文、75 个目录页），旧错误已清除，新的 stderr 为空。实际抓取和保存核验见 `resume.json`，实时状态见 [监控](../source_monitor/latest.md)。NBS 持续运行，SAFE 保持本轮归档完成状态。
