# One-time scripts

These folders preserve dated ingestion, staging, probe, and audit programs that
are useful for reproducing a specific report or database change. They are not
called by the unattended daily or weekly pipeline.

Run a script only with its matching report and archived inputs available. A
dated folder may contain hard-coded evidence hashes, periods, or output paths
that are intentionally unsuitable for general production use.
