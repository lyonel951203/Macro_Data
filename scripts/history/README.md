# Historical maintenance scripts

These scripts reproduce completed archive parsing, NBS/SAFE reviews, and old
gap-search batches. Their default manifests now live under `config/history/`.
They are retained for auditability and are not called by the four active
scheduled tasks.
