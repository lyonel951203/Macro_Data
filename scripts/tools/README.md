# Operator tools

These scripts generate inventories, audit data, calibrate availability,
ingest reviewed Wind files, verify exports, and send audit mail. They are not
part of the unattended network execution path.

Run them from the repository root so relative database, configuration and
report paths resolve consistently.
